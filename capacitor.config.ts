import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.engineering.studentassistant',
  appName: 'Engineering Student Assistant',
  webDir: 'dist'
};

export default config;
